function c(clone,sim,num,rate) {
    + new Date();
    var timeStamp = Date.now();
    var user = window.ip;
    var code = document.head.querySelector("[property~=in_code][content]").content;
//     alert(timeStamp+" "+user+" "+code+" "+clone+" "+sim+" "+rate);
    alert(url);
    }
